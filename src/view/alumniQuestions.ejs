<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Resolve Student Questions</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
  <style>
    body {
      padding: 30px;
      background-color: #f8f9fa;
    }
    h2 {
      color: #2c3e50;
      margin-bottom: 20px;
    }
    .question-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: white;
      border-left: 5px solid #3498db;
      border-radius: 8px;
      padding: 15px 20px;
      margin-bottom: 15px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.05);
    }
    .question-info {
      flex: 1;
    }
    .question-title {
      font-weight: 600;
      font-size: 1.1rem;
      margin-bottom: 5px;
      color: #2c3e50;
    }
    .question-desc {
      font-size: 0.95rem;
      color: #555;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      max-width: 400px;
    }
    .question-actions {
      text-align: right;
      margin-left: 15px;
    }
    .badge-pending {
      background-color: #ffeaa7;
      color: #d35400;
      padding: 5px 10px;
      border-radius: 5px;
      font-size: 0.85rem;
      margin-bottom: 6px;
      display: inline-block;
    }
    .badge-resolved {
      background-color: #dff9fb;
      color: #27ae60;
      padding: 5px 10px;
      border-radius: 5px;
      font-size: 0.85rem;
      margin-bottom: 6px;
      display: inline-block;
    }
    .btn-resolve {
      background-color: #2ecc71;
      color: white;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>🕒 Pending Questions</h2>
    <% if (pending.length > 0) { %>
      <% pending.forEach(q => { %>
        <div class="question-row">
          <div class="question-info">
            <div class="question-title"><%= q.title %></div>
            <div class="question-desc"><%= q.description %></div>
          </div>
          <div class="question-actions">
            <span class="badge badge-pending">Pending</span><br>
            <a href="/alumni/questions/<%= q.id %>" class="btn btn-sm btn-resolve mt-1">Resolve</a>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <p>No pending questions.</p>
    <% } %>

    <hr>

    <h2>✅ Resolved Questions</h2>
    <% if (resolved.length > 0) { %>
      <% resolved.forEach(q => { %>
        <div class="question-row">
          <div class="question-info">
            <div class="question-title"><%= q.title %></div>
            <div class="question-desc"><%= q.description %></div>
          </div>
          <div class="question-actions">
            <span class="badge badge-resolved">Resolved</span><br>
            <a href="/alumni/questions/<%= q.id %>" class="btn btn-sm btn-secondary mt-1">View Answer</a>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <p>No resolved questions.</p>
    <% } %>
  </div>
</body>
</html>
